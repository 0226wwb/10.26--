void my_strcopy(char* str, char* stt)
{
	while (*str != 0)
	{
		*stt= *str;
		stt++;
		str++;
	}
	*stt = *str;
}
int main()
{
	char arr[] = "gfyvudsjzcs";
	char arr1[] = "$$$$$$$$$$$$$";
	my_strcopy(arr, arr1);
	printf("%s\n", arr1);
	return 0;
}
